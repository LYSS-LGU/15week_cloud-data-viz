# 15주차 클라우드 기반 데이터 시각화 - 스트림릿 대시보드 의존성

# 핵심 라이브러리
streamlit>=1.28.0
pandas
numpy
matplotlib
seaborn
plotly

# 이미지 처리 (autocarz_streamlit.py, app.py용)
Pillow>=9.0.0

# 기존 패키지들
folium
dash
requests
jupyter
nbformat

# Chat Completion API 실습 예제 의존성
# 설치 명령: pip install -r requirements_chat_examples.txt

# 기본 OpenAI API
openai>=1.0.0

# LangChain 관련 패키지
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.1.0

# 환경변수 관리
python-dotenv>=1.0.0

# 비동기 처리 (Python 3.7+ 기본 포함)
# asyncio는 Python 표준 라이브러리

# 추가 유틸리티 (선택사항)
# jupyter  # Jupyter 노트북에서 실행할 경우
# streamlit  # 웹 인터페이스 구축시
